{
  "name": "custom-user-orchestrator",
  "version": "1.0.0",
  "description": "1. Place plugin folder in wp-content/plugins/\r 2. Run `composer install` (to generate autoload)\r 3. Build block JS with `npm install` and `npx wp-scripts build` (if you change block source)\r 4. Activate plugin in WP admin.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "wp-scripts build ./src/Block/index.js --output-path=build --output-filename=block.js",
    "start": "wp-scripts start"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@wordpress/scripts": "^31.1.0"
  }
}
